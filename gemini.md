# RUGBY PICKS プロジェクト憲法

このドキュメントは、プロジェクトの品質と方向性を維持するための永続的なルールを定義します。

## 1. デザインと美学 (Design Aesthetics)
- **プレミアム感の維持**: ダークモード、イエローアクセント（#facc15）、力強いタイポグラフィ（font-black）を基調とした、現代的でかっこいいデザインを維持する。
- **視認性の優先**: 選手名、ポジション、チーム名などの重要情報は一目で分かるように配置する。
- **選手カードの標準化**:
    - 右上のアクセントカラーをイエロー（#facc15）に統一する。
    - チーム名はグレーの枠線（border-gray-200）と薄いグレーの背景（bg-gray-100）のボックスに配置する。
    - フォントサイズは視認性を重視し、チーム名、学校名、代表歴（国flag＋テキスト）を標準より大きく設定する。
- **リーグ・選手名の表示優先順位**:
    - **海外リーグ（SR, Top 14, URC）**:
        - **英語名（name_en / team_en_name）をメイン**として最も大きく表示し、全大文字（uppercase）を適用する。
        - 日本語名（title / team_name）はその下に小さくサブとして表示する。
    - **League One**:
        - **日本語名（title / team_name）をメイン**として最も大きく表示する。
        - 英語名（name_en / team_en_name）はその下に小さくサブとして表示する。
- **海外リーグ（SR, Top 14, URC）のデザイン**:
    - 回遊率向上のため、キャリア履歴内のチーム名は可能な限り詳細ページ（/teams/）へリンクさせる。
    - **リーグ別ブランドカラー**:
        - **URC**: 紺 (#003366) / 水色 (#0097D7)
        - **League One**: 黒 (#000000) / 赤 (#E60012)
        - **Top 14**: 黒 (#000000) / 金 (#C5A059)
        - **Super Rugby**: 青 (#0055A4) / 水色 (#00A9E0)

## 2. データの取り扱い (Data Source of Truth)
- **CSV至上主義**: `data_sources/final_master_data_v17_consolidated.csv` を唯一の正解（Source of Truth）とする。
- **公式サイト優先の原則**: 情報の正確性を期すため、可能な限り各リーグの公式サイト、および各チームの公式サイトからデータを取得する。
    - **League One**: `https://league-one.jp/`
    - **Super Rugby**: `https://super.rugby/`
    - **Top 14**: `https://top14.lnr.fr/`
    - **URC**: `https://www.unitedrugby.com/`
- **チーム個別ページの巡回**: リーグ公式サイトに詳細がない場合、各チームの公式サイト（選手名鑑やSquadページ）まで深掘りして最新の選手情報を取得する。
- **all.rugby の扱い**: 公式サイトからの取得が困難な場合、または補完的な情報が必要な場合にのみ参照する。
- **League One の公式参照**: 試合結果は `https://league-one.jp/schedule/`、順位は `https://league-one.jp/standings/` を正解として参照する。
- **自動計算の原則**:
    - **年齢**: 生年月日から動的に算出・更新する。不完全な形式（例：2005..）の場合でも、パース可能な範囲で年を特定し算出を試みるが、不可能な場合は空欄とし品質レポートに出力する。
    - **代表キャップ**: 公式プロフィールの記述から最新数値を抽出し、自動更新する。
- **順位表の完全性**: 各リーグの順位表データは、全チームの情報が完備されていることを確認してから更新する。通信エラー等で一部のチームしか取得できなかった場合は更新を中断し、不完全なデータをサイトに反映させない。
- **データ不備の報告**: スクレイピング実行後、代表キャップやチーム遍歴が欠落している選手を特定し、品質レポートとして出力する。
- **補完の禁止**: 独自の判断でネット検索やAIによる情報の補完、書き換えを行わない。データがない場合は「なし」としてレポートに出力し、将来の更新に委ねる。
- **特定の命名規則**: 
    - 「Vodacom」は「ヴォーダコム」と表記する（「ヴォダコム」は不可）。
... (represent the rest of section 2)

## 7. UI/UX の柔軟性とレスポンシブ
- **長大名称の処理**: チーム名などが長く、狭いエリア（順位表等）で改行が発生し視認性が損なわれる場合は、フォントサイズの自動調整や、短縮版日本語名への切り替えをフレキシブルに行う。
- **固定幅の回避**: コンテンツの長さに応じてコンテナを適切に拡張、またはテキストを省略せずに美しく収める工夫をする。
- **代表データの抽出**:
    - `代表キャップ数` フィールドから、「（国名）代表」の形式で国名を自動抽出する。
    - 日本代表以外の外国籍選手（NZ, AUS, SA 等）についても、略称を含めて正確に国旗絵文字（FLAG_MAP）と紐付ける。
    - 抽出に失敗した場合は、安易に日の丸をフォールバックとして表示せず、空欄または元のテキストを優先する。
- **キャリア遍歴の整形**:
    - 古い順から新しい順になるように並べる（昇順）。
    - リーグワンのチームは日本語表記とする。
    - 同じチームの期間が連続または重複している場合は、ひとまとめ（例: 2020-2024）にする。
    - 現在所属しているチームは「2025 - 」のように開始年のみを表記する（終了年を空ける）。
- **選手検索の階層化**: 選手一覧の絞り込みフィルターは、最初に「リーグ」を選択し、その配下に「チーム（マルチセレクト）」を配置する構成とする。これにより、膨大なチーム数からの選択負荷を軽減する。

## 3. 技術スタックと自動化 (Architecture & Automation)
- **Astro + React**: 爆速なページ遷移とSEO、インタラクティブな検索機能を両立する。
- **データパイプライン**: CSVの更新が、コマンド一つでウェブサイトの Markdown 生成（`scripts/generate_players.py` 等）に反映される構造を維持する。
- **URLのクリーンさと一意性**:
    - スラッグは英語名ベース（ない場合は player-ID）とする。
    - **IDの重複排除**: 複数のチームに所属する選手などの重複を防ぐため、`all.rugby` の `Scraped_Url` に含まれる固有 ID をベースにしたスラッグ生成を徹底する。
- **チームリンクの自動化**: `scripts/team_utils.py` のマッピングを正解とし、全リーグの Markdown 生成時にキャリア履歴を自動でリンク化する。
- **データ整合性の自動検証**:
    - `scripts/generate_players.py` 実行時に、CSVのデータ行数と生成された Markdown ファイル数が完全に一致することを自動的に検証する。
    - 1件でも欠落がある場合は、スクリプトをエラー終了させ、不完全な状態でのサイト更新を防止する。
- **高精度な検索ロジック**:
    - 数値に関連する属性（年齢、身長、体重）の検索では「歳」「cm」「kg」の単位を付与する。
    - プログラム側でこれらの単位を検知した場合、部分一致ではなく、数値の「完全一致」で絞り込む。これにより、意図しない数値の混入（例：年齢25で125kgがヒットする）を防ぐ。

## 4. プロジェクトの呼称
- サイト名は **RUGBY PICKS** とする。
- ドメインは `https://rugbypick.com` とする。

## 5. 運用とメンテナンス (Operation & Maintenance)
- **更新フローの遵守**: データの更新（キャップ数、移籍等）は必ず `data_sources/final_master_data_v17_consolidated.csv` を編集することで行う。
- **一括同期の強制**: CSV更新後は、必ず `scripts/update_site.sh` を実行して、順位表の自動取得・Markdown生成・Astro同期・キャッシュクリアを一括で行う。これにより、データの不整合やキャッシュによる表示遅延を防止する。
- **定期的なメンテナンス**: リーグ戦の各節終了後、または代表戦の後にCSVを最新化することを推奨する。

## 6. 自動化スクリプトの役割
- `scripts/scrape_standings.py`: 各リーグ公式サイトから最新順位を取得し、反映する。
- `scripts/update_site.sh`: サイト更新に必要な全工程（スクレイピング、生成、同期、クリア）を統合した唯一の実行エントリポイントとする。
- **オートメーション・サイクル**:
    - GitHub Actions を使用し、毎週月曜日の朝にデータの自動取得と更新を行う。
    - 更新があった場合のみ、自動的にコミット＆プッシュされ、本番環境へ反映される。
